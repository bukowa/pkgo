name: tests

on: [push, pull_request]

jobs:

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1

      - name: go test
        run: |
          GO111MODULE=on go test -v ./...